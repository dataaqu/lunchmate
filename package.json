{
  "name": "tbilisi-food-guide",
  "version": "0.0.0",
  "private": true,
  "description": "თბილისის კვების გიდი — ვებ პლატფორმა ტურისტებისთვის",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "db:generate": "pnpm --filter @tfg/db generate",
    "db:migrate": "pnpm --filter @tfg/db migrate"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@11.1.3"
}
